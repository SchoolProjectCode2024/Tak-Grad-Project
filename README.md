# Tak-Grad-Project
A platform for playing the game Tak.

## dependencies
add librabies into pyproject.toml
    dependencies
    $ uv sync
        -apply dependencies changes

$ uv run tak
    -launch project
